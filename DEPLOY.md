# نشر نظام متابعة العقود — V20.2.0

## ملاحظة البناء

تم تجهيز المشروع بحيث لا يعتمد على تثبيت Wrangler من npm أثناء `bun install`. إعدادات Cloudflare موجودة في `wrangler.toml`.

## إعداد Cloudflare Workers

1. اربط مستودع Git بمشروع Cloudflare Workers.
2. اجعل Root Directory هو مجلد المشروع الذي يحتوي `wrangler.toml`.
3. تأكد من وجود D1 binding باسم `DB`.
4. تأكد من أن `database_id` في `wrangler.toml` يشير إلى قاعدة D1 الصحيحة.
5. لا تستخدم `bun run deploy` كـBuild Command.
6. إذا كانت بيئة Cloudflare تطلب Build Command للمشروع فقط، يمكن تركه فارغاً؛ الـAssets موجودة في `public` ومحددة في `wrangler.toml`.
7. انشر الـWorker بالطريقة الأصلية للبيئة المرتبطة بالمستودع.

## ترقية D1

V20 تحتوي ترقية تلقائية من نموذج الأقاليم القديم إلى نموذج المسؤول المباشر.

عند أول طلب بعد النشر يقوم Worker بترحيل البيانات، ثم يرفع `schema_meta` إلى الإصدار 23.

لا تحذف قاعدة D1 ولا تنشئ قاعدة جديدة؛ الترحيل مصمم للحفاظ على المعاملات والمستخدمين وسجل النشاط.

## التحقق بعد النشر

افتح:

- `/api/health` — يجب أن يظهر `version: 23` وأن يحتوي جدول `records` على `responsible_user_id` ولا يحتوي على `region`.
- `/` — يجب أن يظهر **V20.2.0**.
- شاشة المستخدمين — يجب أن يظهر دور **مسؤول** بدون أي حقل إقليم.
- شاشة رفع المعاملات — يجب اختيار المسؤول مباشرة.
- تحليل الأداء — يجب أن يحتوي فقط على المؤشرات الخمسة ومنطقة المقارنة.
