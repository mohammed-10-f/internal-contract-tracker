# نشر V14.1 على Cloudflare Workers + D1

## 1) قبل النشر

ملف `wrangler.toml` يحتوي حاليًا على:

- Account ID: `b1e41da21e7671a9c76745f82207d68a`
- D1 Database ID: `291a956f-045d-4d2e-ace5-6cf83b3f6690`
- D1 Database Name: `internal-contract-tracker-db`

إذا كان حساب Cloudflare أو قاعدة D1 مختلفة عندك، استبدل القيمتين فقط بالقيم الفعلية من لوحة Cloudflare.

## 2) التثبيت والتسجيل

```bash
npm install
npx wrangler login
```

## 3) تطبيق Migration

```bash
npx wrangler d1 migrations apply internal-contract-tracker-db --remote
```

> Worker يحتوي أيضًا على حماية Schema ذاتية للتوافق مع قاعدة موجودة مسبقًا، لذلك لا تحذف البيانات القديمة.

## 4) النشر

```bash
npx wrangler deploy
```

## 5) اختبار V14.1

1. تسجيل الدخول.
2. الرئيسية.
3. حساب مسؤول الإقليم: يعرض إحصائيات آخر 7 أيام.
4. المعاملات.
5. فتح المعاملة.
6. الإفادة السريعة من بطاقة المعاملة لمسؤول الإقليم.
7. الاعتماد من خارج تفاصيل المعاملة.
8. الإسناد من داخل المعاملة فقط.
9. المستخدمون والتفويض.
10. إنشاء مستخدم.
11. تعديل صلاحيات مستخدم وحفظها.
12. تعطيل/تفعيل مستخدم.
13. إضافة/تعديل/تعطيل/تفعيل إقليم.
14. التفويض لمعاملات المصدر النشطة والمستقبلية.
15. تحليلات الأداء.
16. التصدير بالتاريخ.
17. سجل النشاط والتصدير.
18. تنزيل نموذج Excel.
19. رفع XLSX/CSV.

## خطأ D1 10021

إذا ظهر:

`binding DB of type d1 must have a valid database_id specified [code: 10021]`

فتأكد أن `database_id` في `wrangler.toml` هو **Database ID الحقيقي** وليس اسم قاعدة البيانات، وأن حساب النشر هو نفس الحساب الذي تظهر فيه قاعدة D1.

إذا كانت قاعدة D1 في حساب مختلف، غيّر `account_id` و`database_id` في الملف قبل النشر.
