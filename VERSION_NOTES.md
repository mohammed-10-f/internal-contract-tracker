# V20.0.0 — إعادة هندسة الربط وإلغاء مفهوم الإقليم

- إلغاء مفهوم الإقليم من واجهة النظام ومنطق الأعمال.
- حذف شاشة الأقاليم بالكامل.
- لم تعد المعاملة تحتوي على إقليم أو تعتمد عليه في أي قرار أو فلترة.
- ربط كل معاملة مباشرة بالمستخدم المسؤول عبر `responsible_user_id`.
- تحويل حسابات مسؤول الإقليم القديمة إلى دور `responsible` تلقائياً أثناء ترقية قاعدة البيانات.
- ترحيل `region_user_id` القديم إلى `responsible_user_id` ثم إعادة بناء الجداول لإزالة الأعمدة الإقليمية من المخطط الحي.
- تحديث حالات سير العمل إلى `waiting_responsible` و`responsible_documented` و`responsible_withdrawn`.
- تحديث سجل المراحل إلى مرحلة `responsible`.
- إزالة كيان دليل الأقاليم وأي عمليات إضافة/أرشفة/إعادة تفعيل للأقاليم.
- نموذج رفع المعاملات الفردي يختار المسؤول مباشرة.
- نموذج Excel الجماعي يستخدم عمود «المسؤول» ويتم مطابقة الاسم أو اسم المستخدم مع الحسابات النشطة.
- شاشة المستخدمين أصبحت المصدر الوحيد لإدارة حسابات المسؤولين وتفعيلها/تعطيلها.
- إعادة بناء شاشة تحليل الأداء لتعرض فقط: إجمالي المعاملات، تم التوثيق، منسحب الموظف، المعاملات المتأخرة، أُغلقت بعد التأخير.
- إضافة منطقة مقارنة واضحة لأداء المستخدمين، وتستخدم نفس فترة التاريخ المختارة لجميع المستخدمين.
- إزالة الرسوم والمؤشرات الإضافية من شاشة تحليل الأداء لتصبح أكثر مباشرة وقراراً.
- رفع الإصدار إلى V20.0.0.
